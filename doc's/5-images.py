from py2html.main import *

page = web()
page.create()

# Images Parameters
#   image = location of the image to be used
#   image = image to insert
#   caption = if the image does not load show caption
page.image(image=, caption=)

page.compile()